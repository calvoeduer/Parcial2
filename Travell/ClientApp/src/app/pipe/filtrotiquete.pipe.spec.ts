import { FiltrotiquetePipe } from './filtrotiquete.pipe';

describe('FiltrotiquetePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrotiquetePipe();
    expect(pipe).toBeTruthy();
  });
});
