import { HandleHttpErrorService } from './handle-http-error-service';

describe('HandleHttpErrorService', () => {
  it('should create an instance', () => {
    expect(new HandleHttpErrorService()).toBeTruthy();
  });
});
